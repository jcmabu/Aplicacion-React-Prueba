import React from 'react';

const AuthLayout = ({children}) => {
    return (
        <div>
            layout autenticacion
            {children}
        </div>
    )
    
};

export default AuthLayout;
